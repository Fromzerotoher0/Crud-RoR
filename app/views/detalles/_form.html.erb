<%= form_with(model: detalle) do |form| %>
  <% if detalle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(detalle.errors.count, "error") %> prohibited this detalle from being saved:</h2>

      <ul>
        <% detalle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
  <div class="row justify-content-center">
	<div class="col-12 col-md-8 col-lg-6 pb-5">
          <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad , class:"form-control col-md-9"%>
  </div>

  <div class="field">
    <%= form.label :Prestamo_id %>
    <%= form.text_field :Prestamo_id , class:"form-control col-md-9" %>
  </div>

  <div class="field">
    <%= form.label :libro_id %>
    <%= form.text_field :libro_id , class:"form-control col-md-9"%>
  </div>

 <div class="actions">
        <%= form.submit nil, :class => 'btn btn-primary' %>
      </div>
<% end %>
  </div>
  </div>
  </div>
  
